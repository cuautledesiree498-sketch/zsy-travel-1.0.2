# ZSY Travel Phase 5C Task: Dynamic Detail Pages Metadata / Schema

## Role boundary
You are OpenCode. Execute only the code implementation requested here.

Important:
- Do not make product, business, legal, pricing, refund, qualification, licensing, partner, address, phone, or payment-policy decisions.
- Do not invent company licenses, supplier resources, public phone numbers, WhatsApp, addresses, guarantees, fixed prices, fixed deposits, or fixed refund/cancellation rules.
- Do not rewrite page marketing copy unless required for metadata/schema consistency.
- Do not edit Sanity schemas or content data.
- Do not edit long-term memory files.
- Do not commit.

## Project
Path:
`C:\Users\Administrator\travel-website\my-travel-site`

## Phase goal
Phase 5 is SEO / Schema / Search readiness.

Phase 5A completed shared SEO utilities and homepage/global metadata.
Phase 5B completed core static page metadata/schema.

This chunk, Phase 5C, should add cautious metadata and limited schema to dynamic detail pages only.

## Pages in scope
Edit only these pages:
- `app/articles/[slug]/page.tsx`
- `app/tours/[slug]/page.tsx`
- `app/destinations/[slug]/page.tsx`

Do not edit:
- static pages from Phase 5B
- legal pages
- Sanity schemas
- memory files
- article/tour/destination source content files unless strictly required for type/build correctness

## Context files to read first
Read before editing:
1. `C:\Users\Administrator\.openclaw\workspace\memory\projects\zsytravel\roadmap-2026-04-25\OPENCODE_WORKFLOW.md`
2. `C:\Users\Administrator\.openclaw\workspace\memory\projects\zsytravel\roadmap-2026-04-25\progress.md`
3. `C:\Users\Administrator\.openclaw\workspace\memory\projects\zsytravel\zsytravel-phase3-xiaxia-judgement-2026-04-25.md`
4. `lib/seo.ts`
5. `components/JsonLd.tsx`
6. `lib/i18n.ts`
7. `lib/sanity.ts`
8. `lib/featuredCases.ts`
9. `lib/destinationContent.ts`
10. each in-scope page before editing it

## Business / wording boundary
The site must remain cautious:
- Infinite Travel / 无限旅途 is a China custom travel planning / tailor-made journey service.
- Route cases and destination pages are references / starting points, not fixed products.
- Final itinerary, quote, payment and cancellation terms depend on dates, group size, route, service scope and written confirmation.
- Payment should remain after route, dates, group size, service scope and confirmation are clear.

Do not write or imply:
- confirmed travel agency license / licensed travel agency
- official guarantee
- guaranteed booking / guaranteed refund
- fixed prices unless an existing real field is already present and already rendered
- fixed deposit percentage
- fixed refund or cancellation fee
- public phone / WhatsApp / address unless already present in page data
- named partners or supplier resources
- TravelAgency schema
- Product / Offer schema for route cases, because route cases are not fixed bookable products

Use neutral schema types only:
- `BreadcrumbList`
- `BlogPosting` for articles only
- `ItemList` only if a page visibly renders a list and it is safe

## Required implementation

### A. Shared searchParams / params handling
Use Next.js 16 compatible async handling.
For each page, safely read:
- `params.slug`
- `searchParams.lang`

Follow patterns already used in Phase 5A/5B pages.

### B. Dynamic metadata
For each in-scope page, add/improve `generateMetadata()` with:
- language-aware title
- cautious description from visible source fields/fallbacks
- canonical + alternates using `buildLocalizedAlternates(path, lang)`
- OpenGraph title/description/url/type
- Twitter title/description/card
- image only if safely available as a public absolute URL; otherwise skip image rather than forcing one

Important path handling:
- `/articles/${encodeURIComponent(slug)}`
- `/tours/${encodeURIComponent(slug)}`
- `/destinations/${encodeURIComponent(slug)}`

### C. `app/articles/[slug]/page.tsx`
Add:
- Breadcrumb JSON-LD: Home -> Insights -> Article title
- BlogPosting JSON-LD using `buildArticleJsonLd()`

Use only existing article fields:
- title
- excerpt/tagline
- mainImage if safe to convert to absolute/public URL through existing image helpers
- author
- publishDate
- url

If author/date missing, omit or use safe fallback only if already visibly rendered. Do not invent expert credentials.

Do not change PortableText rendering or article UI.

### D. `app/tours/[slug]/page.tsx`
Add:
- Breadcrumb JSON-LD: Home -> Route Cases/Tours -> current route case

Optional:
- If useful and safe, add ItemList JSON-LD from visible itinerary days only. This should describe itinerary steps as a list, not sell as a product.

Do not add:
- Product schema
- Offer schema
- TravelAgency schema
- fixed price metadata unless already present and already rendered

Metadata description should clearly treat this as a reference route / route case, not a fixed package.

### E. `app/destinations/[slug]/page.tsx`
Add:
- Breadcrumb JSON-LD: Home -> China Destinations -> current destination

Optional:
- If useful and safe, add ItemList JSON-LD from visible sample plan or highlights only.

Metadata description should treat the page as destination inspiration / route planning reference, not a fixed product.

### F. Keep UI stable
Do not redesign pages.
Do not change CTA logic.
Do not alter legal/payment/quote wording except metadata/schema.

## Validation
Run:
`npm run build`

If build fails, fix build errors within allowed scope.

Also run:
`git diff --stat`

## Reply format
Write final report to `.opencode/reply.txt` with:

1. Changed files
2. Summary of edits by dynamic page
3. Build result
4. `git diff --stat` summary
5. Boundary checks
6. Any skipped schema/image fields with reason
7. Risks or follow-up needed

Do not commit.
