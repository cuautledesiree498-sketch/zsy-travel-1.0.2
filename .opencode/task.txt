Task: Fix the visible English wording seam on the public Services page with the smallest safe edit.

Project root:
C:\Users\Administrator\travel-website\my-travel-site

Target file:
- app/services/page.tsx

Goal:
On the live site, each service card currently renders an awkward duplicate English phrase like:
- "Choose this when: Choose this when you want..."
This needs to be cleaned up.

Allowed change scope:
- Edit ONLY `app/services/page.tsx`
- Edit ONLY the English `chooseWhen` values inside the `services` array
- Keep Chinese copy unchanged
- Keep page structure, metadata, layout, CTA, route logic, imports, and all other text unchanged
- Do NOT edit `pageIntro`, `chooseGuideTitle`, `chooseGuideItems`, card titles, descriptions, durations, examples, or CTA text

Required replacements:
1) Group Tours
Change English `chooseWhen` from:
`Choose this when you want one coordinated structure, easier budget control, and less friction across multiple travelers.`
To:
`You want one coordinated structure, easier budget control, and less friction across multiple travelers.`

2) Educational Tours
Change English `chooseWhen` from:
`Choose this when the trip needs more than sightseeing and requires program themes, visit structure, and group coordination.`
To:
`The trip needs more than sightseeing and requires program themes, visit structure, and group coordination.`

3) Private Tailor-Made Trips
Change English `chooseWhen` from:
`Choose this when pacing, hotel style, experience order, and personal preferences matter more than a standard format.`
To:
`Pacing, hotel style, experience order, and personal preferences matter more than a standard format.`

4) Corporate Travel Solutions
Change English `chooseWhen` from:
`Choose this when the schedule needs to work around meetings, visits, hosting standards, and tight time windows.`
To:
`The schedule needs to work around meetings, visits, hosting standards, and tight time windows.`

Validation:
- Run `npm run build`
- If build passes, write a concise result into:
  - `.opencode/reply.txt`

Reply format for `.opencode/reply.txt`:
- State that only `app/services/page.tsx` was edited
- State that only the 4 English `chooseWhen` strings were cleaned up
- State that build passed
- State that no other content or structure was changed
