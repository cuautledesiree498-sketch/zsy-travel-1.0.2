# Task: Apply first-pass Home page hero and trust-copy cleanup

## Goal
Apply a first-pass human-written copy cleanup to the Home page hero and trust sections. This is a low-risk copy refinement task focused on making the homepage feel less like a polished travel template and more like a sharp, precise, and attractive entry point.

## File to edit
- `app/page.tsx`

## Hard constraints
- Read the real file first before editing.
- Only edit `app/page.tsx`.
- Do not change page structure.
- Do not touch metadata.
- Do not touch the `trustDecisionItems` array.
- Do not touch the `highlightItems` array.
- Do not touch CTA button label text.
- Do not touch layout, routing, CMS, About, Services, Contact, FAQ, Payment, API, or unrelated files.
- Keep the existing bilingual `en` / `zh` structure.
- Keep changes minimal and low-risk.
- Run `npm run build` after editing.
- Write the final execution report to:
  - `C:\Users\Administrator\travel-website\my-travel-site\.opencode\reply.txt`

## Required changes

Only replace these 8 parts in `app/page.tsx`:

1. `heroTitle`
2. `heroSubtitle`
3. `heroSupporting`
4. `trustSectionEyebrow`
5. `trustSectionTitle`
6. `trustSectionBody`
7. `highlightSectionTitle`
8. `highlightSectionBody`

Do not change anything else unless required to keep TypeScript/JSX valid.

---

## 1) Replace `heroTitle`

Replace with:

```tsx
const heroTitle = lang === 'zh' ? '把中国旅程先理顺。' : 'Get Your China Trip Straightened Out First.';
```

---

## 2) Replace `heroSubtitle`

Replace with:

```tsx
const heroSubtitle = lang === 'zh' ? '面向家庭、私人小团、研学和商务来华客人的定制旅行。' : 'Tailor-made China travel for families, private groups, study trips, and business guests.';
```

---

## 3) Replace `heroSupporting`

Replace with:

```tsx
const heroSupporting = lang === 'zh' ? '先把日期、人数和重点偏好告诉我们，我们会先理清路线方向，再往下做。' : 'Share your dates, group size, and priorities, and we’ll shape the route from there.';
```

---

## 4) Replace `trustSectionEyebrow`

Replace with:

```tsx
const trustSectionEyebrow = lang === 'zh' ? '为什么更好判断' : 'Why It’s Easier to Decide';
```

---

## 5) Replace `trustSectionTitle`

Replace with:

```tsx
const trustSectionTitle = lang === 'zh' ? '先把路线判断对。' : 'Start With the Right Route.';
```

---

## 6) Replace `trustSectionBody`

Replace with:

```tsx
const trustSectionBody = lang === 'zh' ? '我们会根据你的时间、人数和重点偏好，先判断路线是否顺、节奏是否合适、后面的确认会不会卡住。' : 'We look at your dates, group size, and priorities first, so it’s easier to see whether the route flows well, the pace makes sense, and the confirmation process will stay clear.';
```

---

## 7) Replace `highlightSectionTitle`

Replace with:

```tsx
const highlightSectionTitle = lang === 'zh' ? '适合想先把方向理清的人' : 'For Travelers Who Want the Direction Clear First';
```

---

## 8) Replace `highlightSectionBody`

Replace with:

```tsx
const highlightSectionBody = lang === 'zh' ? '适合不想上来就套模板，而是先确认路线值不值得做的人。' : 'Best for travelers who do not want a copied template and prefer to confirm whether the route is worth doing first.';
```

---

## Validation
After editing:

1. Run `npm run build`.
2. Write the execution report to `.opencode/reply.txt` with:
   - Summary
   - Files changed
   - What changed in each file
   - Key diff snippets or inserted copy blocks
   - Build result
   - Errors / risks / unfinished items
   - Anything that still needs human confirmation later

## Expected result
The homepage hero and trust sections should feel sharper, more precise, and more attractive, while still staying concise and avoiding generic AI-style travel copy.
